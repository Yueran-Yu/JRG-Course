## Setting - Run jQuery in js file
1. initialize ```package.json```, ```yarn init -y```
2. Add jQuery: ```yarn add jQuery```
3. Add ```/.cache/``` and ```/node_modules/``` to the ```.gitignore```file

